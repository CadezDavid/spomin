services:
  spomin:
    build: .
    ports:
      - "8084:8084"
    env_file: .env
    volumes:
      - spomin_data:/app/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

volumes:
  spomin_data:
